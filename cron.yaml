cron:
- description: create html,xml,Map.db from spread job
  url: /crons/crawl_spread
  schedule: every 1 minutes

- description: create max|min_lat|lng from map kml job
  url: /crons/crawl_map
  schedule: every 1 hours
